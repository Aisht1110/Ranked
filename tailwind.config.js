module.exports = {
  content: [
    "./*.html",        // ✅ all HTML files in the root folder
    "./*.js"           // ✅ any root-level JS files (optional, but useful)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
